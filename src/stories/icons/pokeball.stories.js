import React from "react";

import { Pokeball } from "../../icons/";

export default {
  title: "Icons/Pokeball",
  component: Pokeball,
};

const Template = (args) => <Pokeball {...args} />;

export const Default = Template.bind({});

